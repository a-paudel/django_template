[project]
name = "django_template"
version = "0.1.0"
description = "Default template for PDM package"
authors = [
    {name = "Ankur Paudel", email = "ankur@paudel.me"},
]
dependencies = [
    "django>=5.0.6",
    "jinja2>=3.1.4",
]
requires-python = "==3.12.*"
readme = "README.md"
license = {text = "MIT"}


[tool.pdm]
distribution = false
