from django import forms
from core.forms import BaseForm
from {{app_name}}.models import {{model_name}}

class {{model_name}}CreateForm(BaseForm, forms.ModelForm):
    class Meta:
        model = {{model_name}}
        fields = {{model._meta.get_fields()|selectattr("editable")|map(attribute="name")|list}}

class {{model_name}}UpdateForm(BaseForm, forms.ModelForm):
    class Meta:
        model = {{model_name}}
        fields = {{model._meta.get_fields()|selectattr("editable")|map(attribute="name")|list}}